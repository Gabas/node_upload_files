var arr = ["apple", "banana", "carpet", "bin", "drill"];

var max = {};

for (var i = 0; i < arr.length; i++) {

	for (var j = 0; i < arr.length; j++) {
		if (arr[i].charAt(0) == arr[j].charAt(0)) {
			
		}
	}

}